% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tab_baseline.R
\name{tab_baseline}
\alias{tab_baseline}
\title{Baseline characteristics table with standardized mean differences}
\usage{
tab_baseline(
  data,
  by,
  vars,
  labels = NULL,
  type = NULL,
  by_missing = c("error", "level", "drop"),
  missing_text = "Missing",
  overall = TRUE,
  overall_label = "Overall",
  max_levels = 20L,
  digits_continuous = 1L,
  digits_pct = 1L,
  digits_smd = 3L,
  quantile_type = 2L,
  big_mark = "",
  stat_prefix = ""
)
}
\arguments{
\item{data}{A data frame (or tibble) with one row per subject.}

\item{by}{Name of the grouping variable, as a single string. One column of
the table is produced per level. Factor levels are kept in their declared
order; levels with no rows are dropped, and how many were dropped is
reported in the diagnostics.}

\item{vars}{Names of the row variables, as a character vector, in the order
they are to appear. Must not contain \code{by}.}

\item{labels}{Labels for the row variables: a named character vector or
named list, e.g. \code{c(age = "Age at diagnosis, years")}. Variables with no
entry are labelled with their column name. Entries for variables not in
\code{vars} are ignored, so one project-wide label list can be passed to every
table.}

\item{type}{Optional named character vector overriding how a variable is
summarised, e.g. \code{c(dx_year = "continuous", stage = "categorical")}.
Allowed values are \code{"continuous"} and \code{"categorical"}. Without an
override, numeric variables are continuous and factor, character and
logical variables are categorical. Every name must be in \code{vars}.}

\item{by_missing}{What to do with rows whose \code{by} value is missing:
\code{"error"} (the default) to stop, \code{"level"} to give them their own column
labelled \code{"(Missing)"}, or \code{"drop"} to leave them out. The number of such
rows is reported in the diagnostics in all three cases.}

\item{missing_text}{Row label used for the count of missing values.}

\item{overall}{Whether to prepend a column describing all groups together.}

\item{overall_label}{Name of that column.}

\item{max_levels}{Largest number of categories a variable may have before
the call stops. The check is on the number of rows the variable would
produce: the number of factor levels, or the number of distinct
non-missing values for character, logical and numeric columns.}

\item{digits_continuous, digits_pct, digits_smd}{Decimal places for the
median and quartiles, for percentages, and for the formatted SMD. Ties
are rounded away from zero, not half to even.}

\item{quantile_type}{Which of the nine algorithms of \code{\link[stats:quantile]{stats::quantile()}}
computes the quartiles. Defaults to \code{2}, the SAS definition used by
\code{gtsummary::tbl_summary()}; pass \code{7} for R's own default. The median is
the same under both.}

\item{big_mark}{Thousands separator for counts, passed to
\code{\link[base:formatC]{base::formatC()}}. Defaults to \code{""}, i.e. none; house styles that write
\verb{12 345} or \verb{12,345} pass \code{" "} or \code{","} here rather than having the
convention baked into the package.}

\item{stat_prefix}{Optional prefix for the group column names, e.g. \code{"n_"}.
Use it when a group level would otherwise collide with one of the fixed
column names.}
}
\value{
A plain data frame with one row per table row and the columns
\itemize{
\item \code{variable}, \code{var_label}, \code{var_type} -- the row variable, its label, and
whether it was summarised as continuous or categorical;
\item \code{row_type} -- \code{"label"}, \code{"level"} or \code{"missing"};
\item \code{label} -- what to print in the first column of the table;
\item \code{stat_label} -- what the cells of that row hold (\code{"Median [IQR]"},
\code{"n (\%)"}, \code{"n"});
\item one character column per group, named after the group level, preceded by
the overall column when \code{overall = TRUE};
\item \code{smd}, \code{smd_fmt}, \code{smd_pair}, \code{smd_n} -- the standardized mean
difference, filled on the \code{"label"} row of each variable and \code{NA}
elsewhere.
}

Two attributes are attached:
\itemize{
\item \code{smd_table} -- one row per variable, with \code{smd_max}, \code{smd_pair}, \code{smd_n}
and the \code{method} used;
\item \code{tab_baseline} -- a list with \code{counts} (rows read, rows used, rows with a
missing group), \code{group_n} (the column denominators), \code{missing} (missing
counts and percentages by variable and group), \code{smd}, \code{checks} (the
invariants that were enforced), \code{settings} and \code{call}.
}

Attributes are dropped by most data-frame verbs, so read them off the
object returned by \code{tab_baseline()} before piping it further.
}
\description{
\code{tab_baseline()} builds the table a cohort paper prints first: one block of
rows per variable, one column per group, continuous variables summarised as
median \link{IQR} and categorical variables as n (\%), plus the standardized mean
difference (SMD) between the groups.

The row variables, the grouping variable and the row labels are arguments,
so the function is not tied to any particular registry export. The function
prints nothing and writes nothing: it returns a plain data frame, and the
per-variable missing counts, the group sizes and the SMD bookkeeping are
attached to it as attributes.
}
\section{What is reported}{
Each row variable contributes a block of rows:
\itemize{
\item a \code{"label"} row, which for a continuous variable carries the statistic
\code{median [p25, p75]} and for a categorical variable is a heading with
empty cells;
\item one \code{"level"} row per category, carrying \verb{n (\%)}, for categorical
variables only. Factor levels are kept in their declared order, including
levels with no observations; character and logical variables are
tabulated in sorted order;
\item a \code{"missing"} row carrying the number of missing values, present when the
variable has at least one missing value anywhere in the data used. That
row deliberately carries a count and no percentage.
}

A variable with two categories gets both rows. It is never collapsed to a
single "n (\%) of the second level" row, which is what
\code{gtsummary::tbl_summary()} does by default: a reader who can see only one
of the two rows cannot see where the denominator went, and cannot tell a
variable with no missing values from one with many.
}

\section{Rounding, and which quartile}{
Two conventions are worth stating, because they decide the last printed
digit:
\itemize{
\item quartiles come from \code{stats::quantile(..., type = quantile_type)}, and the
default is \code{type = 2}, the SAS definition, which is what
\code{gtsummary::tbl_summary()} uses. R's own default is \code{type = 7}; the two
differ in the quartiles but never in the median. Pass
\code{quantile_type = 7} for R's default. Whichever is used is recorded in the
\code{settings} diagnostic, so a table always carries its own definition;
\item ties are rounded \strong{away from zero}, so \verb{53.25\%} prints as \verb{53.3\%}. Base
R's \code{round()} rounds half to even and would print \verb{53.2\%}. The rule used
here is \code{trunc(abs(x) * 10^d + 0.5 + sqrt(.Machine$double.eps)) / 10^d},
the same rule as \code{gtsummary}'s \code{style_number()}, the epsilon being what
makes a decimal literal stored just below a tie, such as \code{0.145}, round
up rather than down.
}

With these two settings the cells reproduce
\code{gtsummary::tbl_summary(percent = "column", missing = "ifany")} exactly.
}

\section{Percentages}{
The denominator of a percentage is \strong{the number of non-missing values of
that variable in that column}, not the column total. Missing values
therefore never dilute the percentages: they are shown on their own row and
counted in the \code{missing} diagnostic. This is the convention of the study
script this function was distilled from, and of
\code{gtsummary::tbl_summary(percent = "column", missing = "ifany")}. A column in
which every value of a variable is missing has no estimable percentage, so
its cells are \code{NA}.
}

\section{Standardized mean difference}{
The SMD is an effect size and, unlike a p-value, does not grow with the
sample size, which is why it is the statistic to read in an observational
Table 1. For two groups:
\itemize{
\item continuous: \code{d = (m1 - m2) / sqrt((s1^2 + s2^2) / 2)};
\item categorical: the multi-category generalisation of Yang and Dalton (2012),
\code{d = sqrt(t(p1 - p2) \%*\% solve(S) \%*\% (p1 - p2))}, where \code{p} holds the
proportions of the first K-1 categories, \code{S = (S1 + S2) / 2} and
\code{S_k = diag(p_k) - tcrossprod(p_k)}. At K = 2 this reduces to the usual
\verb{|p1 - p2| / sqrt((p1 (1 - p1) + p2 (1 - p2)) / 2)}, and that identity is
re-checked on every call and reported in the \code{checks} diagnostic.
}

With more than two groups the reported value is the \strong{largest absolute SMD
over all pairs of groups}, and \code{smd_pair} names the pair; taking the
maximum keeps one badly imbalanced pair from being diluted by the others.
SMDs are computed on non-missing values only and \code{smd_n} reports how many
rows that was. Differential missingness is deliberately not folded into the
SMD: a single number mixing "the distributions differ" with "the
missingness differs" cannot be read back apart. The missing rows are there
for that.
}

\section{What is deliberately not reported}{
No p-values. In an observational cohort the groups were never randomised,
so the null hypothesis of exactly equal distributions is known to be false
before the data are seen, and whether it is rejected is driven by the group
sizes. If a journal insists, compute the tests separately and join them
onto the returned data frame by \code{variable}; keeping them out of this
function means no test is ever chosen on your behalf.
}

\section{Input checks}{
The call stops, with the offending columns, values or counts listed, when
\enumerate{
\item a required column (\code{by}, or any element of \code{vars}) is not in \code{data}, or
appears in \code{data} more than once;
\item \code{by} also appears in \code{vars}, \code{vars} is empty, or \code{vars} has duplicates;
\item \code{by} has missing values and \code{by_missing = "error"} (the default). The
other settings keep those rows as their own column or drop them, and the
number of rows involved is reported in the returned diagnostics either
way -- rows are never dropped without a count;
\item \code{by} has fewer than two non-missing levels: there is nothing to compare
and every SMD would be undefined;
\item a row variable is missing in every row, so no statistic is estimable.
Drop it from \code{vars} and report its missingness on its own;
\item a variable summarised as categorical would produce more than
\code{max_levels} rows, the usual sign of a continuous or identifier column
reaching the table by mistake;
\item a column has a class the function does not summarise (dates, lists), or
a group level would collide with one of the fixed column names of the
result.
}
}

\examples{
## A small hand-built cohort. No real patient records are used anywhere
## in this package.
set.seed(20260901)
toy <- data.frame(
  site = factor(rep(c("Stomach", "Small intestine", "Colorectal"),
                    times = c(50, 30, 20)),
                levels = c("Stomach", "Small intestine", "Colorectal")),
  age  = round(c(rnorm(50, 66, 12), rnorm(30, 62, 13), rnorm(20, 70, 10))),
  sex  = factor(sample(c("Male", "Female"), 100, TRUE, prob = c(.55, .45)),
                levels = c("Male", "Female")),
  size = round(c(rlnorm(50, 3.6, .6), rlnorm(30, 4, .6), rlnorm(20, 3.8, .6)))
)
toy$size[c(3, 17, 45, 88)] <- NA          # a few unrecorded tumour sizes

tab <- tab_baseline(
  toy, by = "site", vars = c("age", "sex", "size"),
  labels = c(age = "Age at diagnosis, years", sex = "Sex",
             size = "Tumour size, mm")
)
tab

## Diagnostics travel with the result instead of being printed.
attr(tab, "smd_table")
attr(tab, "tab_baseline")$group_n
subset(attr(tab, "tab_baseline")$missing, variable == "size")
attr(tab, "tab_baseline")$checks

## An identifier column is not a categorical variable, and the number of
## rows it would produce is checked before anything is tabulated.
toy$record_id <- sprintf("R\%03d", seq_len(nrow(toy)))
try(tab_baseline(toy, by = "site", vars = c("age", "record_id")))

## A grouping variable with one non-missing level has nothing to compare.
try(tab_baseline(toy[toy$site == "Stomach", ], by = "site", vars = "age"))

## A misspelled column name is named in the error message.
try(tab_baseline(toy, by = "site", vars = c("age", "sexe")))

}
\references{
Yang D, Dalton JE. A unified approach to measuring the effect size between
two groups using SAS. SAS Global Forum 2012, paper 335-2012.
}
