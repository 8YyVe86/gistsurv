% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_competing.R
\name{fit_competing}
\alias{fit_competing}
\title{Competing risks: cumulative incidence, Gray's test and Fine-Gray models}
\usage{
fit_competing(
  data,
  time = "time_mo",
  event_cr = "event_cr",
  event_os = "event_os",
  group = NULL,
  covariates = NULL,
  causes = c(1, 2),
  cause_labels = NULL,
  censor_code = 0,
  times = c(12, 36, 60),
  times_beyond = c("error", "na"),
  group_missing = c("error", "level", "drop"),
  missing_text = "Missing",
  labels = NULL,
  na_action = c("omit", "fail"),
  empty_levels = c("drop", "error"),
  cif_complete_case = TRUE,
  cause_specific = c("fit", "skip"),
  conf_level = 0.95,
  epv_warn = 10,
  digits_hr = 2L,
  digits_p = 3L
)
}
\arguments{
\item{data}{A data frame (or tibble) with one row per subject.}

\item{time}{Name of the follow-up-time column, as a single string. Must be
numeric. Factors and character digits are refused: use \code{\link[=prep_surv]{prep_surv()}},
which converts them and returns a numeric \code{time_mo}.}

\item{event_cr}{Name of the competing-risk outcome column, as a single
string. Must hold \code{censor_code} for a censored subject and one of
\code{causes} for a death, and nothing else.}

\item{event_os}{Name of the overall-survival event indicator (\code{0}
censored, \code{1} dead of any cause), as a single string, used only for the
cross-check against \code{event_cr}. \code{NULL} skips the cross-check; the column
is not used for anything else. Defaults to \code{"event_os"}, so the
cross-check happens unless it is switched off on purpose.}

\item{group}{Name of the grouping variable for the cumulative incidence
functions and Gray's test, as a single string, or \code{NULL} (the default)
for one set of curves over the whole cohort. To cross two variables,
build the interaction first, e.g.
\code{data$site_age <- interaction(data$site, data$age_grp, sep = " / ")}.
\code{group} does not enter the Fine-Gray model unless it is also named in
\code{covariates}.}

\item{covariates}{Names of the Fine-Gray covariates, as a character vector,
in the order they should appear in the result, or \code{NULL} (the default) to
estimate cumulative incidence only and fit no model. A factor enters as
its levels against the first level; a numeric column enters as one term
per unit.}

\item{causes}{The cause codes used in \code{event_cr}, as a numeric vector of at
least two whole numbers, none of them equal to \code{censor_code}. Defaults to
\code{c(1, 2)}. Every cause listed must occur in the data, and every death in
the data must carry one of them.}

\item{cause_labels}{Labels for \code{causes}, filling the \code{cause_label} column:
a character vector as long as \code{causes} and in the same order, or one
named by the cause codes. Defaults to \code{"Cause 1"}, \code{"Cause 2"}, and so
on.}

\item{censor_code}{The value of \code{event_cr} that means censored. Defaults to
\code{0}.}

\item{times}{Time points, in the unit of \code{time}, at which the cumulative
incidence, its confidence interval and the number at risk are reported.
Defaults to 12, 36 and 60.}

\item{times_beyond}{What to do with a time point past the largest follow-up
time of a group: \code{"error"} (the default) to stop, or \code{"na"} to keep the
row, mark it \code{estimable = FALSE} and leave the estimate and its interval
missing. There is no setting that extrapolates.}

\item{group_missing}{What to do with rows whose \code{group} value is missing:
\code{"error"} (the default) to stop, \code{"level"} to give them their own group
labelled \code{"(Missing)"}, or \code{"drop"} to leave them out. The number of such
rows is reported in the diagnostics in all three cases.}

\item{missing_text}{Label used for the group of rows with a missing \code{group}
value when \code{group_missing = "level"}.}

\item{labels}{Labels for the covariates, filling the \code{var_label} column: a
named character vector or named list, \code{covariate = "label"}. Covariates
not named keep their column name.}

\item{na_action}{What to do with rows that have a missing covariate value:
\code{"omit"} (the default) to fit the models on the complete cases, or
\code{"fail"} to stop. The counts are reported either way, and the cumulative
incidence is estimated on every usable row in both cases.}

\item{empty_levels}{What to do with a factor level of a covariate that no
row has: \code{"drop"} (the default) to drop it with a warning and a count, or
\code{"error"} to stop. A level emptied by casewise deletion is an error
whatever this is set to.}

\item{cif_complete_case}{Whether to estimate the cumulative incidence a
second time on the complete cases, so that casewise deletion can be shown
to have changed the cause structure or not to have changed it. Ignored
when \code{covariates} is \code{NULL}. Defaults to \code{TRUE}.}

\item{cause_specific}{Whether to fit the cause-specific Cox model beside
each Fine-Gray model: \code{"fit"} (the default) or \code{"skip"}. Ignored when
\code{covariates} is \code{NULL}.}

\item{conf_level}{Confidence level of every interval returned, and, through
\code{1 - conf_level}, the level at which \code{attr(x, "fg_vs_csh")} calls a term
significant. Defaults to \code{0.95}.}

\item{epv_warn}{Smallest number of events of a cause per model coefficient
that does not draw a warning. Fewer events than coefficients is an error
whatever this is set to. Set to \code{0} to silence the warning. Defaults to
\code{10}.}

\item{digits_hr}{Decimal places used when a hazard ratio and its interval
are pasted into \code{shr_txt} and \code{hr_txt}. The numeric columns are returned
unrounded, so rounding stays a presentation choice and never a stored
one.}

\item{digits_p}{Decimal places used for the formatted p-value columns; at
the default a p-value below 0.001 is written \code{"<0.001"}.}
}
\value{
A plain data frame of cumulative incidence estimates, one row per analysis
set, cause, group and time point, with the columns
\itemize{
\item \code{grouping} -- the name of the grouping variable, or \code{"overall"};
\code{analysis_set} -- \code{"full"} or \code{"complete_case"}; \code{group} -- the level, or
\code{"ALL"};
\item \code{cause}, \code{cause_label} -- the cause code and its label;
\item \code{time} -- the time point;
\item \code{n_group}, \code{n_risk}, \code{n_event_cause}, \code{n_event_cause_by_time} -- subjects
in the group, subjects still at risk at \code{time}, events of this cause in
the group at any time, and events of this cause by \code{time};
\item \code{cif}, \code{cif_se}, \code{cif_lcl}, \code{cif_ucl} -- the cumulative incidence, its
standard error and its confidence interval;
\item \code{ci_method}, \code{estimable} -- how the interval was built, or why there is
none;
\item \code{gray_stat}, \code{gray_df}, \code{gray_p}, \code{gray_p_fmt} -- Gray's test for that
cause across all groups, repeated on every row of that cause, \code{NA} when
there is a single group.
}

Six attributes are attached:
\itemize{
\item \code{finegray} -- the Fine-Gray results: one row per cause and covariate
level, reference levels included, with \code{shr}, \code{ci_low}, \code{ci_high},
\code{shr_txt}, \code{coef}, \code{se}, \code{z}, \code{p_value}, \code{p_fmt}, the subjects and events
at that level, and the model-level \code{converged}, \code{loglik}, \code{n_model},
\code{n_full} and \code{n_dropped}. \code{NULL} when \code{covariates} is \code{NULL};
\item \code{cause_specific} -- the same rows for the cause-specific Cox models, with
\code{hr} and the \code{\link[survival:cox.zph]{survival::cox.zph()}} columns that \code{\link[=fit_cox]{fit_cox()}} returns.
\code{NULL} when not fitted;
\item \code{fg_vs_csh} -- the two side by side for every non-reference term, with
\code{both_sig}, \code{same_side} and \code{direction_flip}. \code{NULL} when not fitted;
\item \code{dropna} -- casewise deletion by level of \code{group} plus a total row
(\code{is_total}), with \code{n}, \code{n_dropped}, \code{pct_dropped}, \code{n_dropped_events}
and one \verb{n_dropped_cause<k>} column per cause. \code{NULL} when \code{covariates}
is \code{NULL};
\item \code{outcome_check} -- \code{table}, the \code{table(event_os, event_cr)}
cross-tabulation; \code{counts}, the same as a data frame; \code{checks}, the four
agreement invariants; and \code{note};
\item \code{fit_competing} -- a list with \code{counts}, \code{follow_up}, \code{gray}, \code{missing},
\code{checks}, \code{cause_specific_zph}, \code{settings} and \code{call}.
}

Attributes are dropped by most data-frame verbs, so read them off the
object returned by \code{fit_competing()} before piping it further.
}
\description{
\code{fit_competing()} estimates the cumulative incidence function (CIF) of each
cause of failure, optionally within the levels of one grouping variable,
compares those curves with Gray's test, and fits one Fine-Gray
subdistribution hazard model per cause. It returns the numbers a
competing-risk paper reports: the CIF and its confidence interval at a set
of time points, Gray's test, the subdistribution hazard ratios with their
intervals and p-values, the cause-specific hazard ratios to read beside
them, the rows lost to casewise deletion, and the cross-tabulation of the
overall and the competing-risk outcome column.

The estimators are \code{\link[cmprsk:cuminc]{cmprsk::cuminc()}} and \code{\link[cmprsk:crr]{cmprsk::crr()}}. They are not
interchangeable with \code{riskRegression::FGR()} or with
\code{\link[survival:finegray]{survival::finegray()}} followed by \code{\link[survival:coxph]{survival::coxph()}}: those are different
programmes, and a number that came out of one cannot be traced back to the
other.

The function prints nothing, plots nothing and writes nothing. The CIF
table comes back as a plain data frame and everything else is attached to
it as attributes.
}
\section{Two analysis sets, and why their n differ}{
A cumulative incidence function needs three columns: the follow-up time,
the cause of failure and the grouping variable. A Fine-Gray model needs the
covariates as well, and covariates go missing. The two are therefore
estimated on different sets of rows, deliberately:
\itemize{
\item the CIF, Gray's test and the counts in the returned data frame use every
usable row (\code{analysis_set = "full"});
\item the Fine-Gray models, and the cause-specific Cox models beside them, use
the complete cases (\code{analysis_set = "complete_case"}).
}

The two n are not meant to match, and a table that quotes both must say
which is which -- hence the \code{analysis_set} column on every row of every
table this function returns. The rows lost are counted in
\code{attr(x, "dropna")}, in total and per level of \code{group}, split by cause,
because a loss that falls unevenly across groups is not missing completely
at random and belongs in the limitations of the paper rather than in a
footnote. With \code{cif_complete_case = TRUE} the CIF is estimated a second
time on the complete cases, so that the question "did casewise deletion
change the cause-of-death structure?" has an answer instead of an
assumption.
}

\section{Confidence intervals for the cumulative incidence}{
\code{\link[cmprsk:timepoints]{cmprsk::timepoints()}} returns the CIF and its variance. A Wald interval
built from them, \verb{F +/- z * SE(F)}, has to be truncated into \verb{[0, 1]}, and
in a cell with few events the lower limit is pinned at exactly 0; in a cell
with no events the interval collapses to \verb{[0, 0]}. Declaring such an
interval truncated does not make it a confidence interval.

The interval used here is built on the complementary log-log scale:

\deqn{\nu = \log(-\log(1 - F)), \qquad
      SE(\nu) = \frac{SE(F)}{|(1 - F)\log(1 - F)|}, \qquad
      CI = 1 - \exp(-\exp(\nu \mp z \cdot SE(\nu))).}

The transform maps \verb{(0, 1)} onto the whole real line, so the
back-transformed limits fall strictly inside \verb{(0, 1)} without any
truncation, and the interval is asymmetric where the estimate is small.
That the limits lie strictly inside \verb{(0, 1)} and bracket the point estimate
is checked on every call.
}

\section{Cells where no interval exists}{
The transform is undefined at \code{F = 0} and at \code{F = 1}, and there is nothing
to transform when the estimate itself is missing. Such a cell gets
\code{cif_lcl = cif_ucl = NA}, \code{estimable = FALSE}, and a \code{ci_method} that says
which of the three it is:
\itemize{
\item \code{"not estimable (no events at this horizon)"} -- the group has had no
event of that cause by that time, so \code{cif} is 0. This is the cell to
report as "0 events, CI not estimable", with the number still at risk
from \code{n_risk} beside it. It is never reported as \code{0.00 (0.00-0.00)};
\item \code{"not estimable (cumulative incidence is 1)"} -- everyone in the group
has failed from that cause by that time;
\item \code{"not estimable (the interval degenerates at 0 or 1)"} -- the transform is
defined, but in floating point the back-transformed limits land on the
boundary: an estimate the product-limit arithmetic returns as \code{1 - 2e-16},
or one whose standard error dwarfs it, drives \code{exp(-exp(.))} to 0 or to 1.
The resulting \code{0.000-1.000} carries no information and is the degenerate
interval this transform was chosen to avoid, so it is withdrawn rather
than printed;
\item \code{"not estimable (beyond the largest follow-up time in this group)"} --
only reachable with \code{times_beyond = "na"}; see below.
}

\code{cif == 0} and \code{n_event_cause_by_time == 0} are checked against each other
on every call, in both directions, so a cell labelled "no events" is one.
That an estimable interval lies strictly inside \verb{(0, 1)} and brackets its
estimate is then true by construction, and is checked as well.
}

\section{Time points, and the follow-up guard}{
A cumulative incidence function is not estimable past the largest follow-up
time in a group. \code{fit_competing()} refuses to report one: a time point past
the largest follow-up of any group stops the call, and \code{times_beyond = "na"} keeps the row, marks it \code{estimable = FALSE} and leaves the estimate
and its interval missing. The number still at risk is reported for such a
row, because an empty risk set is a fact rather than an extrapolation. This
is the same rule as the tau guard of a restricted-mean analysis, and the
largest usable time point comes back as \code{follow_up["tau_upper"]} in the
diagnostics.
}

\section{The design matrix that goes into crr()}{
\code{\link[cmprsk:crr]{cmprsk::crr()}} takes a numeric matrix, not a formula, so the dummy coding
is the caller's responsibility and its failure modes are silent: a column
in the wrong position attaches a coefficient to the wrong level, a
reference level dropped by \code{\link[=droplevels]{droplevels()}} moves the reference without
changing any code, and a level emptied by casewise deletion leaves an
all-zero column. The matrix built here is checked before it is used:
\enumerate{
\item it is built with \code{contr.treatment} contrasts, forced for the duration of
the call, so the reference is the first level whatever
\code{options("contrasts")} is set to session-wide;
\item the intercept is dropped and the remaining column names must equal, in
order, the non-reference levels of each covariate;
\item no reference level may appear as a column, and no column name may be
duplicated;
\item every dummy column must be 0/1, and the mean of every column must equal
the proportion of the complete cases at that level -- a continuous
covariate must reproduce its own mean -- to a tolerance of 1e-12. A
shifted column fails this immediately;
\item after the fit, the names of the coefficients returned by \code{crr()} must
equal the column names of the matrix, and \code{crr()} must report the same
number of rows, no rows dropped for missing values, and convergence.
}
}

\section{Subdistribution and cause-specific hazards}{
The Fine-Gray model estimates a subdistribution hazard ratio. It keeps
subjects who died of the competing cause in the risk set, which is what
makes it a model for the cumulative incidence -- and what makes its
coefficients easy to misread as risks. When the competing cause is strong,
a covariate that raises death from cause 1 can show a subdistribution
hazard ratio below 1 for cause 2 simply because those subjects are being
used up by cause 1.

The remedy is not to hide such coefficients but to put the cause-specific
hazard ratio beside them, so \code{fit_competing()} fits both by default. The
cause-specific model is an ordinary Cox model with the competing causes
treated as censoring; it is fitted by \code{\link[=fit_cox]{fit_cox()}} on exactly the same
complete cases and with the same reference levels, which is why it is
fitted here rather than left to the caller -- rebuilding the same
complete-case set by hand is where the two stop being comparable. Going
through \code{\link[=fit_cox]{fit_cox()}} also brings the \code{\link[survival:cox.zph]{survival::cox.zph()}}
proportional-hazards test with it, which the project rules require of every
Cox model. Pass \code{cause_specific = "skip"} to leave it out; any message
raised while fitting it names \code{\link[=fit_cox]{fit_cox()}}, because that is the function
that raised it.

The two models answer different questions and do not have to agree.
\code{attr(x, "fg_vs_csh")} puts them side by side and flags the terms where
both are significant and the directions differ; that flag marks a term to
explain, not a bug to fix.
}

\section{The outcome cross-check}{
\code{event_os} (0 alive or censored, 1 dead of any cause) and \code{event_cr} (the
censoring code, or a cause of death) are two encodings of the same fact and
must agree, in both directions:
\itemize{
\item every censored row of \code{event_os} must carry the censoring code in
\code{event_cr}, and every row carrying the censoring code must be censored in
\code{event_os};
\item every death in \code{event_os} must carry a cause in \code{event_cr}, and every row
carrying a cause must be a death in \code{event_os}.
}

A disagreement stops the call and the offending counts are listed. The
cross-tabulation itself, a tidy version of it and the four checks come back
in \code{attr(x, "outcome_check")}. Pass \code{event_os = NULL} to skip the
cross-check; that is a deliberate act and is recorded in the result.
}

\section{Reproducibility}{
Neither \code{\link[cmprsk:cuminc]{cmprsk::cuminc()}} nor \code{\link[cmprsk:crr]{cmprsk::crr()}} uses random numbers:
\code{cuminc()} is a closed-form estimator and \code{crr()} is a Newton-Raphson
iteration started from zero. Nothing in this function draws from the random
number generator, so the result does not depend on the seed and the state
of the generator is left untouched.
}

\section{Input checks}{
The call stops, with the offending columns, values or counts listed, when
\enumerate{
\item a required column is not in \code{data}, or appears in \code{data} more than once;
\item \code{time} is not numeric, or holds missing, negative or infinite values;
\item \code{event_cr} holds a value that is neither \code{censor_code} nor one of
\code{causes}, missing values included. The offending values are listed with
their counts;
\item a cause in \code{causes} never occurs in \code{data}. \code{cuminc()} drops such a
cause from its output altogether and a Fine-Gray model for it has
nothing to fit;
\item \code{event_os} and \code{event_cr} disagree in either direction;
\item \code{group} has missing values and \code{group_missing = "error"} (the default),
or has fewer than two levels with rows in them;
\item a covariate is of a class that cannot enter the model, is constant, is
an ordered factor, or is missing in every row;
\item casewise deletion empties a level of a covariate, or leaves fewer events
of some cause than the model has coefficients;
\item the design matrix does not encode the levels it is supposed to encode,
or \code{crr()} does not converge;
\item an element of \code{times} is past the largest follow-up time of some group
and \code{times_beyond = "error"} (the default), or \code{times} holds
duplicated, missing, negative or infinite values.
}
}

\examples{
## A small simulated cohort. No real patient records are used anywhere in
## this package.
set.seed(20260901)
n    <- 600
site <- factor(rep(c("Stomach", "Small intestine", "Colorectal"),
                   length.out = n),
               levels = c("Stomach", "Small intestine", "Colorectal"))
age  <- factor(sample(c("<65", "65+"), n, TRUE), levels = c("<65", "65+"))
sex  <- factor(sample(c("Male", "Female"), n, TRUE),
               levels = c("Male", "Female"))
r1   <- c(Stomach = 0.006, "Small intestine" = 0.011,
          Colorectal = 0.016)[as.character(site)]
r2   <- ifelse(age == "65+", 0.012, 0.003)
t1   <- stats::rexp(n, r1)
t2   <- stats::rexp(n, r2)
tc   <- stats::runif(n, 6, 130)
tm   <- pmin(t1, t2, tc)
toy  <- data.frame(
  site     = site,
  age      = age,
  sex      = sex,
  time_mo  = round(tm, 1),
  event_os = as.integer(tm < tc),
  event_cr = ifelse(tm == tc, 0L, ifelse(t1 <= t2, 1L, 2L))
)
## a covariate with missing values, to exercise casewise deletion
toy$stage <- factor(sample(c("Localised", "Advanced"), n, TRUE),
                    levels = c("Localised", "Advanced"))
toy$stage[sample(n, 60)] <- NA

## Cumulative incidence only, one set of curves for the whole cohort.
ci <- fit_competing(toy, cause_labels = c("Death from GIST",
                                          "Death from other causes"))
ci[, c("cause_label", "time", "cif", "cif_lcl", "cif_ucl", "n_risk")]

## The outcome cross-check is returned, not printed.
attr(ci, "outcome_check")$table
attr(ci, "outcome_check")$checks

## By primary site, with Gray's test repeated on every row of a cause.
cif <- fit_competing(toy, group = "site",
                     cause_labels = c("Death from GIST",
                                      "Death from other causes"))
cif[cif$time == 60, c("group", "cause_label", "cif", "cif_lcl", "cif_ucl",
                      "gray_p_fmt")]

## With covariates: Fine-Gray models, the cause-specific models beside
## them, and the rows casewise deletion costs.
fg <- fit_competing(toy, group = "site",
                    covariates = c("site", "age", "sex", "stage"),
                    cause_labels = c("Death from GIST",
                                     "Death from other causes"),
                    labels = c(site = "Primary site", age = "Age group"))
attr(fg, "finegray")[, c("cause_label", "variable", "level", "n",
                         "n_event_cause", "shr_txt", "p_fmt")]
attr(fg, "dropna")

## The two hazards side by side; a flagged term is one to explain.
attr(fg, "fg_vs_csh")[, c("cause_label", "term", "shr_txt", "hr_txt",
                          "both_sig", "same_side", "direction_flip")]

## Diagnostics travel with the result instead of being printed.
attr(fg, "fit_competing")$counts
attr(fg, "fit_competing")$follow_up
attr(fg, "fit_competing")$gray
attr(fg, "fit_competing")$checks

## A cell with no events of a cause has no interval, and says so rather
## than reporting 0.00 (0.00-0.00).
recoded <- toy
recoded$event_cr[recoded$site == "Colorectal" & recoded$event_cr == 2L] <- 1L
z <- fit_competing(recoded, group = "site", times = c(12, 60))
z[z$cause == 2 & z$group == "Colorectal",
  c("group", "time", "n_risk", "n_event_cause_by_time", "cif", "cif_lcl",
    "ci_method")]

## A time point past the end of follow-up is refused, not extrapolated.
try(fit_competing(toy, group = "site", times = c(12, 60, 200)))

## An impossible cause code is listed, not silently read as censored.
bad <- toy
bad$event_cr[c(1, 5, 9)] <- 3L
try(fit_competing(bad, group = "site"))

## So is a disagreement between the two outcome columns.
bad2 <- toy
bad2$event_os[bad2$event_cr == 2L][1:4] <- 0L
try(fit_competing(bad2, group = "site"))

}
\seealso{
\code{\link[=fit_cox]{fit_cox()}}, which fits the cause-specific models here and is the
right function for an overall-survival Cox model; \code{\link[=fit_km]{fit_km()}} and
\code{\link[=calc_rmst]{calc_rmst()}} for the all-cause view of the same cohort; \code{\link[=prep_surv]{prep_surv()}},
which builds \code{time_mo}, \code{event_os} and \code{event_cr} from vital-status
columns.
}
