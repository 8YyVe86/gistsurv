% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_surv.R
\name{prep_surv}
\alias{prep_surv}
\title{Prepare survival and competing-risk outcome variables}
\usage{
prep_surv(
  data,
  time = "time_mo",
  status = "vital_status",
  alive_value = "Alive",
  dead_value = "Dead",
  cause = NULL,
  cause_gist_value = NULL,
  cause_other_value = NULL,
  cause_unknown_value = NA,
  cause_censor_value = NULL,
  cause_unknown_to = 2L
)
}
\arguments{
\item{data}{A data frame (or tibble) with one row per subject. The class
of \code{data} is preserved; the new columns are appended on the right.}

\item{time}{Name of the follow-up-time column, as a single string. Its
values may be numeric or character digits (a zero-padded registry export
such as \code{"0012"} is accepted); factors are refused, because factor
levels are not numbers.}

\item{status}{Name of the vital-status column, as a single string.}

\item{alive_value, dead_value}{Values of \code{status} that mean alive
(censored) and dead. Each may be a vector if several codes are used.
The two sets must not overlap, and together they must cover every value
present in the column.}

\item{cause}{Name of the cause-of-death column, as a single string, or
\code{NULL} (the default) for overall survival only. When \code{NULL}, no
\code{event_cr} column is produced.}

\item{cause_gist_value}{Values of \code{cause} that mean death from the disease
of interest (mapped to \code{event_cr = 1}). Required whenever \code{cause} is
supplied.}

\item{cause_other_value}{Values of \code{cause} that mean death from another
cause (mapped to \code{event_cr = 2}). Leave \code{NULL} to accept any undeclared
cause as a death from other causes; supply it to force every admissible
value to be declared.}

\item{cause_unknown_value}{Values of \code{cause} that mean the cause of death
is unknown. Defaults to \code{NA}, i.e. a missing cause of death.}

\item{cause_censor_value}{Values of \code{cause} that mean "no event", for a
cause column that already carries its own censoring code. A subject
recorded as dead that carries one of these values is an error.}

\item{cause_unknown_to}{Category an unknown cause of death is assigned to:
\code{1L} (death from the disease of interest) or \code{2L} (death from other
causes). \code{0L} is refused.}
}
\value{
\code{data} with \code{time_mo} and \code{event_os} appended, plus \code{event_cr} when
\code{cause} was supplied, and with two attributes:
\itemize{
\item \code{os_cr_table} -- the \code{table(event_os, event_cr)} cross-check, or \code{NULL}
when no \code{cause} was supplied;
\item \code{prep_surv} -- a list with \code{counts} (subjects, censored, deaths, and the
\code{event_cr} categories including how many rows an unknown cause was
recoded into), \code{follow_up} (minimum, median, maximum and the number of
zero follow-up times), \code{checks} (the invariants that were enforced),
\code{settings} (the column names and value sets used) and \code{call}.
}

Attributes are dropped by most data-frame verbs, so read them off the
object returned by \code{prep_surv()} before piping it further.
}
\description{
\code{prep_surv()} takes a data frame with one row per subject and returns it
with the columns a survival analysis needs:
\itemize{
\item \code{time_mo} -- follow-up time, numeric, non-missing, non-negative;
\item \code{event_os} -- overall-survival indicator, \code{1} = death, \code{0} = censored;
\item \code{event_cr} -- competing-risk outcome, \code{0} = censored,
\code{1} = death from the disease of interest, \code{2} = death from other causes
(only produced when \code{cause} is supplied).
}

Column names and coding values are arguments, so the function is not tied
to any particular registry export. Every recoding rule is explicit: a
value the function has not been told about stops the call instead of being
folded into "censored" or "other cause". The function prints nothing; the
\code{event_os} by \code{event_cr} cross-tabulation and the remaining diagnostics
are attached to the returned data frame as attributes.
}
\section{Recoding rules}{
\code{event_os} is \code{0} for rows whose \code{status} value is in \code{alive_value} and
\code{1} for rows whose \code{status} value is in \code{dead_value}. Any other value,
including \code{NA}, is an error.

\code{event_cr} is derived from \code{status} and \code{cause} together, so that the two
outcome variables cannot contradict each other by construction:
\itemize{
\item alive -> \code{0};
\item dead and \code{cause} in \code{cause_gist_value} -> \code{1};
\item dead and \code{cause} in \code{cause_unknown_value} -> \code{cause_unknown_to};
\item dead and \code{cause} in \code{cause_other_value} -> \code{2};
\item dead and none of the above -> \code{2} when \code{cause_other_value} is \code{NULL}
(open coding: any undeclared cause is a death from other causes), and an
error when \code{cause_other_value} is supplied (closed coding: the set of
admissible causes has been declared in full).
}

The four value sets must not overlap. \code{cause_unknown_value} defaults to
\code{NA}, i.e. a dead subject with a missing cause of death is treated as an
unknown cause and sent to \code{cause_unknown_to}; the number of rows recoded
this way is reported in the returned attributes, because it is the figure
a limitations paragraph has to quote.
}

\section{Input checks}{
The call stops, with the offending values listed, when
\enumerate{
\item a required column (\code{time}, \code{status}, \code{cause}) is not in \code{data};
\item \code{time} is missing, negative, infinite, or not readable as a number;
\item \code{status} or \code{cause} holds a value outside the declared sets;
\item \code{status} and \code{cause} contradict each other -- a subject recorded as
alive that carries a declared cause of death, or a subject recorded as
dead that carries a \code{cause_censor_value}. \code{cause_unknown_to = 0} is
refused for the same reason: it would censor a dead subject in
\code{event_cr} while \code{event_os} records a death.
}

The bidirectional \code{event_os} / \code{event_cr} agreement (\code{event_os == 0} only
with \code{event_cr == 0}; \code{event_os == 1} only with \code{event_cr} in \code{1:2}; and
the count of deaths equal to the sum of the two competing-risk events) is
verified again on the derived columns before the result is returned.
}

\examples{
## A small hand-built cohort. No real patient records are used anywhere
## in this package.
toy <- data.frame(
  id    = 1:8,
  fu    = c(0, 5, 12, 18, 24, 36, 48, 60),
  vital = c("Dead", "Dead", "Alive", "Dead", "Alive", "Dead", "Alive", "Dead"),
  cod   = c("GIST", "Heart disease", NA, "Unknown", NA, "GIST", NA, "Stroke"),
  stringsAsFactors = FALSE
)

## Overall survival only.
os <- prep_surv(toy, time = "fu", status = "vital")
os[, c("id", "time_mo", "event_os")]

## Competing risks: "GIST" is the event of interest, "Unknown" and a
## missing cause are sent to "death from other causes".
cr <- prep_surv(toy, time = "fu", status = "vital",
                cause = "cod", cause_gist_value = "GIST",
                cause_unknown_value = c("Unknown", NA),
                cause_unknown_to = 2L)
cr[, c("id", "time_mo", "event_os", "event_cr")]

## The cross-check and the counts travel with the result.
attr(cr, "os_cr_table")
attr(cr, "prep_surv")$counts

## Closed coding: every admissible cause is declared, so a value that was
## not declared stops the call instead of becoming "other cause".
try(prep_surv(toy, time = "fu", status = "vital",
              cause = "cod", cause_gist_value = "GIST",
              cause_other_value = "Heart disease",
              cause_unknown_value = c("Unknown", NA)))

## A misspelled column name is named in the error message.
try(prep_surv(toy, time = "fu", status = "vital_status"))

}
